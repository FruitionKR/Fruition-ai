fastapi
uvicorn[standard]
python-multipart

# MVP starts with PDF conversion only.
# Extend later as needed, for example:
#   markitdown[pdf,docx,pptx,xlsx]
markitdown[pdf]

# document-restoration runtime
# CPU-only torch: PyPI linux wheels pull ~6GiB of nvidia/cuda/triton packages
# that exhausted EKS node disk during image extraction. The official CPU index
# ships +cpu wheels for x86_64 and aarch64; docling-ibm-models needs torch>=2.2.2,<3.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.14.0+cpu
torchvision==0.29.0+cpu
docling>=2.0
Pillow>=10.0
PyMuPDF>=1.24
pypdfium2>=5.12
pix2tex>=0.1.4

# Range-readable PDF page extraction; whole source files are never buffered.
pypdf>=6.13,<7
